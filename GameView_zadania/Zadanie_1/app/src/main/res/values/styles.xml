<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="hpBar" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/progress_hp</item>
        <item name="android:minHeight">5dp</item>
        <item name="android:maxHeight">25dp</item>

    </style>
    <style name="staminaBar" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/progress_stamina</item>
        <item name="android:minHeight">5dp</item>
        <item name="android:maxHeight">25dp</item>

    </style>

    <style name="xpBar" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/xp_progressbar</item>
        <item name="android:minHeight">5dp</item>
        <item name="android:maxHeight">25dp</item>

    </style>
</resources>
